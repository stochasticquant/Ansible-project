# Ansible-project

This project contains Ansible playbooks for configuring Linux VMs.
`install_apache_playbook.yml` installs Apache and deploys a simple
biography website from `bio-website/index.html` to `/var/www/html` on the
target hosts.
